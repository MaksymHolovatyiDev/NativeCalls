import { StyleSheet } from 'react-native';
import { colors } from '../../shared/colors';

export const styles = StyleSheet.create({
  local: { flex: 1, backgroundColor: colors.Black },
  remote: {
    flex: 1,
    backgroundColor: colors.Black,
    marginTop: 8,
  },
});
